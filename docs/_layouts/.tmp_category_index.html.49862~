---
layout: default
---
<div class="category-archive">
  <div>
    <span class="title">Category archive for {{ page.category }}</span>
  </div>
  <div>
    <ul>
		{{ content }}
		
		{% for cat in site.categories %}
	            {% if cat[0] == page.category %}
				
					{% for post in cat.last %}
				      <li>
				        <span class="post-meta">发布时间：{{ post.date | date: '%Y-%m-%d' }}</span>
						<span class="post-meta">分类：{{ post.category }}</span>
						<span class="post-meta">
							作者：<img alt="@kelseyschimm" class="author-avatar" data-user="4154342" height="18" src="https://avatars0.githubusercontent.com/u/11390617?v=3&s=460" width="18">
							kelseyschimm 
						</span>
						<span class="post-meta">
							字数：{{ page.content | number_of_words }}
						</span>
				        <h2>
				          <a class="post-link" href="{{ post.url | prepend: site.baseurl }}">{{ post.title }}</a>
				        </h2>
						<p>{{ post.excerpt }}</p>
						<!--<p>{{ post.content }}</p>-->
						
				      </li>
					  
				    {% endfor %}
					
	            {% endif %}
	      {% endfor %}
		
		
    </ul>
  </div>
</div>
